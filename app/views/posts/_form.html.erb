<%= form_for @post do |f| %>
<% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this abum from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    
    <%= f.label :title %> <br>
    <%= f.text_field :title %> <br>
    <br>
    <br>
    <%= f.label :content, "Тест Вашей новости здесь!" %> <br>
    <%= f.text_area :content %>
        <br>
    <br>
    
    <div class="field">
    <%= f.label :images_files %><br>
    <%= f.attachment_field :images_files, multiple: true, presigned: true, direct: true %>
</div>

    <%= f.submit %>

<% end %>